package Servicios;

public class EmailService {
    public void enviarMail(String destinatario, String asunto, String mensaje) {
        System.out.println("Enviando mail a " + destinatario + " con asunto: " + asunto);
        // Lógica real de envío en versión web
    }
}
